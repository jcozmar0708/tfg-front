export const selectResponse = (state) => state.payments.response;

export const selectSuccess = (state) => state.payments.success;

export const selectLoading = (state) => state.payments.loading;

export const selectError = (state) => state.payments.error;